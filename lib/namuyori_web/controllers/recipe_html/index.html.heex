<!--<.header>
  Listing Recipes
  <:actions>
    <.link href={~p"/recipes/new"}>
      <.button>New Recipe</.button>
    </.link>
  </:actions>
</.header>

<.table id="recipes" rows={@recipes} row_click={&JS.navigate(~p"/recipes/#{&1}")}>
  <:col :let={recipe} label="Title"><%= recipe.title %></:col>
  <:col :let={recipe} label="Description"><%= recipe.description %></:col>
  <:col :let={recipe} label="Preparation time"><%= recipe.preparation_time %></:col>
  <:col :let={recipe} label="Cooking time"><%= recipe.cooking_time %></:col>
  <:col :let={recipe} label="Total time"><%= recipe.total_time %></:col>
  <:col :let={recipe} label="Dificulty level"><%= recipe.dificulty_level %></:col>
  <:col :let={recipe} label="Url image"><%= recipe.url_image %></:col>
  <:col :let={recipe} label="Url video"><%= recipe.url_video %></:col>
  <:action :let={recipe}>
    <div class="sr-only">
      <.link navigate={~p"/recipes/#{recipe}"}>Show</.link>
    </div>
    <.link navigate={~p"/recipes/#{recipe}/edit"}>Edit</.link>
  </:action>
  <:action :let={recipe}>
    <.link href={~p"/recipes/#{recipe}"} method="delete" data-confirm="Are you sure?">
      Delete
    </.link>
  </:action>
</.table>-->
